# Jekyll configuration for prajitdas.github.io
# Minimal public configuration

# Basic exclusions only
exclude:
  - .github/
  - "*.py"
  - "*.pyc"
  - "*.sh"
  - __pycache__/
  - .pytest_cache/
  - node_modules/

# Site settings (if needed)
plugins:
  - jekyll-sitemap